var status = -1;

function start() {
    status = -1;
    action(1, 0, 0);
}

function action() {
	cm.sendOk("Welcome, welcome. I am #bMr. Pickall#k, I can pick any lock, any lock in the world, except the ones I can't. So if you need a lock picked, just bring it to me.");
	cm.dispose();
}