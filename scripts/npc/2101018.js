var status = -1;

function start() {
    action(1, 0, 0);
}

function action(mode, type, selection) {
        cm.sendOk("Sorry, my services are closed to prevent you from getting stuck!");
        cm.dispose();
}
